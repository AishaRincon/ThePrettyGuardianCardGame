// const span = document.getElementById("timer");

// export default class timer {
//     constructor(root) {
//         root.innerHTML = timer.getHTML();

//         this.element = {
//             minutes: root.querySelector(".timer__part--minutes"),
//             seconds: root.querySelector(".timer__part--seconds"),
//             control: root.querySelector(".timer__btn--control"),
//             reset: root.querySelector(".timer__btn--reset"),
//         };
// //Make sure the timer isnt running as soon as the webpage is loaded
//         this.interval = null; 

// //current remaining seconds
//         this.remaining = seconds = 60;
//         this.updateInterfaceTime ();

// //add an event listener for the above controlsl.
// //start/stop button should click start
//         this.element.control.addEventListener("click", () => {

//         });

//         this.element.reset.addEventListener("click", () => {

//         });
//     }

//     updateInterfaceTime() {
//         const minutes = Math.floor(this.remainingSeconds / 60);

//         console.log(minutes, seconds);
//     }}



// //Timer
// const span = document.getElementById("timer");

//     constructor(root);
//         root.innerHTML = timer.getHTML();

//         this.element = {
//             minutes: root.querySelector(".timer__part--minutes"),
//             seconds: root.querySelector(".timer__part--seconds"),
//             control: root.querySelector(".timer__btn--control"),
//             reset: root.querySelector(".timer__btn--reset"),
//         };
// //Make sure the timer isnt running as soon as the webpage is loaded
//         this.interval = null; 

// //current remaining seconds
//         this.remaining = seconds = 60;
//         this.updateInterfaceTime ();

// //add an event listener for the above controlsl.
// //start/stop button should click start
//         this.element.control.addEventListener("click", () => {

//         });

//         this.element.reset.addEventListener("click", () => {

//         });

//     updateInterfaceTime() 
//         const minutes = Math.floor(this.remainingSeconds / 60);

//         console.log(minutes, seconds);